package com.yuanno.block_clover.api;

import com.yuanno.block_clover.api.ability.Ability;
import com.yuanno.block_clover.data.ability.AbilityDataCapability;
import com.yuanno.block_clover.data.ability.IAbilityData;
import net.minecraft.entity.player.PlayerEntity;

public class Bebcapi {

    /*
    private boolean isAbilityActive(PlayerEntity player, Ability ability) {
        IAbilityData abilityData = AbilityDataCapability.get(player);
        Ability equippedAbility = abilityData.getEquippedAbility(ability.getAbilityType());
        return equippedAbility != null && equippedAbility.equals(ability);
    }

     */

}
